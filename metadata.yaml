name: astrbot_plugin_laizhi # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 来只插件 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: AstrBot 来只图库插件 - 支持图片下载、存储和随机分享 # 插件简短描述
version: v2.1.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: mp4502 # 作者
repo: https://github.com/mp4502/astrbot_plugin_laizhi # 插件的仓库地址
dependencies:
  - aiohttp>=3.9.0
  - aiofiles>=23.0.0
